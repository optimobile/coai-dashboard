import { NotificationCenter } from './NotificationCenter';

/**
 * NotificationBell Component
 * Simple wrapper that renders the NotificationCenter dropdown
 */
export default function NotificationBell() {
  return <NotificationCenter />;
}
